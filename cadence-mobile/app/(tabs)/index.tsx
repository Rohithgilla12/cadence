// Today screen scaffold — replaced by the feature subagent's full implementation.
import { Text, View } from 'react-native';

export default function TodayScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-bg">
      <Text className="font-serif text-h1 text-ink">Today</Text>
    </View>
  );
}
